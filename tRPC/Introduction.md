# TypeScript Remote Procedure Call

* tRPC is a framework that lets you call backend functions directly from the frontend 👉 without writing REST APIs or GraphQL schemas
* Not ideal for public APIs -> External developers can’t easily consume it