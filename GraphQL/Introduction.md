* Flexible data fetching (frontend-heavy apps)
* Client decides what data it needs (no over-fetching / under-fetching)
* Single endpoint instead of multiple REST endpoints
* Strongly typed schema
* use case: React apps, dashboards, mobile apps